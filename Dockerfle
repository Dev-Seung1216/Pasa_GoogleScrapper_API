# 파이썬 3.9 버전 사용
FROM python:3.9-slim

# 작업 폴더 설정
WORKDIR /app

# 라이브러리 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 코드 복사
COPY . .

# 서버 실행 명령 (Cloud Run은 $PORT 환경변수를 사용함)
CMD exec uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}
